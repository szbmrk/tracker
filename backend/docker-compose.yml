version: '3.3'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tracker_api
    restart: always
    env_file: 
      - .env
    ports:
      - "${PORT}:${PORT}"